const conf = {
  appWriteUrl: String(VITE_APP_WRITE_URL),
  appWriteProjectId: String(VITE_APPWRITE_PROJECT_ID),
  appDatabaseId: String(VITE_APPWRITE_DATABASE_ID),
  appCollectionId: String(VITE_APPWRITE_COLLECTION_ID),
  appBucketId: String(VITE_APPWRITE_BUCKET_ID),
};

export default conf;
